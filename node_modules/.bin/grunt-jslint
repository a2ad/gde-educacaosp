#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-jslint/bin/grunt-jslint" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-jslint/bin/grunt-jslint" "$@"
  ret=$?
fi
exit $ret

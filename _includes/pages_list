{% for node in pages_list %}
  {% if group == null or group == node.group %}
    {% if page.url == node.url %}
      <li class="current"><a href="{{ site.baseurl }}">{{node.title}}</a>
			<p>{{ site.baseurl }}</p>
			<p>{{node.url}}</p>
      </li>
    {% else %}
      <li><a href="{{ site.baseurl }}">{{node.title}}</a>
      <p>{{ site.baseurl }}</p>
			<p>{{node.url}}</p></li>
    {% endif %}
  {% endif %}
{% endfor %}
{% assign pages_list = nil %}
{% assign group = nil %}